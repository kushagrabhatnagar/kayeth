class operators
{

   public static void main(String args[])
   {
      float x,y,z,a,b,c;
 
       x =5;
      y = 10;
      z = x + y;
      a = x - y;
      b = x * y;
      c = x / y;
      System.out.println("Sum of the integers = " + z );
      System.out.println("diff of the integers = " + a );
      System.out.println("mul of the integers = " + b ); 
      System.out.println("div of the integers = " + c );
  }
}